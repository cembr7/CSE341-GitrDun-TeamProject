// __mocks__/src/database.js
/*module.exports = {
  // The real `connectDB` would open a Mongo connection.
  // In tests we just need a stub that resolves immediately.
  connectDB: jest.fn(() => Promise.resolve()),
};*/